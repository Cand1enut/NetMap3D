node_modules
.DS_Store

# NetMap3D.html is generated by build-portable.js but IS committed on purpose:
# it is the whole point of the portable build — one file, no npm, no toolchain,
# hand it to anyone. Rebuild it before committing app.js/index.html/style.css.
NetMap3D-Portable/
*.zip

# Local project saves
*.netmap.json
# Generated by make-dev.js (which IS committed) — a cache-busted copy of
# index.html for testing, so the browser can't serve a stale app.js.
dev.html
